

#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main()
{  
    char s[1024];
    //gets(s);
    // while (gets(s))
    // {

    // }
    //gets(s);
    fgets(s, 1024, stdin);
}